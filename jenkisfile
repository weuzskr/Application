pipeline {
    agent any

    stages {
        

        stage('Install dependencies') {
            steps {
                // Installer les dépendances Python
                sh 'pip install -r requirements.txt'
            }
        }

       
    }
}
